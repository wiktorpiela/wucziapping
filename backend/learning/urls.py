from django.urls import path
from . import views

app_name = "learning"

urlpatterns = [
    # path("closed-ended-questions/", views.GetClosedEndedQuestions.as_view(), name="learning_cloded_ended_questions"),
    # path("closed-ended-questions-categories/", views.ClosedEndedQuestionsCategories.as_view(), name="cloded_ended_questions_categories"),
    # path("test/", views.Test.as_view()),
]